Postgresql and Pgadmin Install with Docker Compose

All you have to do is run the following command.
``` bash
# Install
docker-compose -f docker-compose-postgres.yaml up -d

# Checking
docker ps 
or 
docker-compose -f docker-compose-postgres.yaml ps

```
